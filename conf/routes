# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

GET     /player/:name               controllers.Player.getPlayerBy(name)
PUT     /player/:name               controllers.Player.newPlayer(name)

GET     /games/:playerName          controllers.Game.getGamesFor(playerName)
GET     /game/:id                   controllers.Game.getGameBy(id)
PUT     /game/:playerName           controllers.Game.newGameFor(playerName)

POST    /move/:gameId/:playerName     controllers.Game.newMoveFor(gameId, playerName)
